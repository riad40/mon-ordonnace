import { StyleSheet } from "react-native"

const patientsListStyles = StyleSheet.create({
    container: {
        backgroundColor: "#fff",
        borderBottomColor: "#ddd",
        borderBottomWidth: 1,
        paddingHorizontal: 20,
        paddingVertical: 10,
        borderTopColor: "#ddd",
        borderTopWidth: 1,
    },
})

export default patientsListStyles
