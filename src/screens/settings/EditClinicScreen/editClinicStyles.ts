import { StyleSheet } from "react-native"

const editClinicStyles = StyleSheet.create({
    container: {
        padding: 20,
        backgroundColor: "#fff",
    },
    formContainer: {
        marginTop: 20,
    },
})

export default editClinicStyles
